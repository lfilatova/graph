(ns graph.core
  (:require
    [graph.generator :refer :all]
    [graph.dijkstra :refer :all]
    [graph.distance  :refer :all]))

; This namespace configured as default to be initiated at REPL startup, thus all tasks can be checked here.
